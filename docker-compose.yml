name: Serv-Publicos
services:
  front_web:
    container_name: Serv-Publicos
    build:
      context: .
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "4200:4200"
    command: npm start -- --host 0.0.0.0 --port 4200 --poll 1000

